# cografya_web

Frontend for the **Coğrafya platform** — an SEO-first geography education site (TR + EN),
built with Next.js (App Router).

This is the placeholder root commit on `main`. Active development happens on `dev`;
the initial scaffold (PR-0w) lands via pull request.
