# Build output & caches
.next/
out/
build/
coverage/

# Dependencies
node_modules/

# Generated
next-env.d.ts
pnpm-lock.yaml
lib/api/schema.ts
# generate:map output — machine-written path data, kept as the generator emits it.
lib/map/tr-provinces.generated.ts
# generate:world-map output — same, for the world (regional) country map.
lib/map/world-countries.generated.ts
# generate:water output — same, for the TR inland water layer. NOT optional: `generate:water:check`
# compares the committed file to the generator's output byte for byte, and Prettier splits the
# one-line shape objects across four lines, so a single `prettier --write` (the pre-commit hook
# runs one) turns a green CI red on a file nobody edited.
lib/map/tr-inland-water.generated.ts
# generate:tr-context output (turkiye-yenileme PR-B) — same reason, same PR #39 incident class:
# `generate:tr-context:check` compares byte for byte and Prettier reflows the one-line shape
# objects.
lib/map/tr-context.generated.ts

# Committed source-data snapshots (GeoJSON, see data/README.md) — kept as fetched/
# filtered from upstream, so Prettier must not reformat them.
data/tr-il-boundaries.geojson
data/world-countries.geojson
data/tr-inland-water.geojson
# Same, for the JRC Global Surface Water derivative. Its features are one-per-line by design
# (see scripts/fetch-tr-jrc-water.mjs) and `generate:water:check` re-derives the artifact from
# it byte for byte, so a stray `prettier --write` from the pre-commit hook would turn a green
# CI red on a file nobody edited — the exact PR #39 incident (commit 7c02553).
data/tr-inland-water-jrc.geojson

# Committed OpenAPI contract artifact (synced from cografya_api; kept byte-identical
# to the api's generator output, so Prettier must not reformat it).
openapi/
